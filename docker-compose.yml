version: "3.9"
services:
  workerdisk:
    build: .
    environment:
      - APP_ID=${APP_ID}
      - API_HASH=${API_HASH}
      - TG_BOT_TOKEN=${TG_BOT_TOKEN} 
      - CHANNEL_ID=${CHANNEL_ID}
      - OWNER_ID=${OWNER_ID}
      - FORCE_SUB_CHANNEL=${FORCE_SUB_CHANNEL}
      
